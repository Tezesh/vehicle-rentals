{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "Install Dependencies and Start App",
			"type": "shell",
			"command": "cd backend && npm install && cd ../frontend && npm install && cd ../backend && npm start",
			"problemMatcher": []
		}
	]
}