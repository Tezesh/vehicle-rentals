# MongoDB connection string (update with your actual DB URI)
MONGO_URI=mongodb+srv://saitejeshg2005:tejesh@cluster0.3ao5ohv.mongodb.net/MYDB?retryWrites=true&w=majority&appName=Cluster0
# JWT secret for signing tokens (use a strong, random value in production)
JWT_SECRET=your_jwt_secret_here
# Port for the backend server (optional, defaults to 5000)
PORT=5000
